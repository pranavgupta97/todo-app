{
  "_comment": "Copy this file to http-client.private.env.json (gitignored). After logging in via browser, copy the JSESSIONID cookie value from DevTools > Application > Cookies > localhost:8080 and paste it as the value below.",
  "dev": {
    "JSESSIONID": "paste-your-jsessionid-from-browser-devtools-here"
  }
}
