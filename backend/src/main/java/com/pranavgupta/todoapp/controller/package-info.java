/**
 * REST controllers. Thin HTTP layer — handles request/response shape and input
 * binding, and delegates all business logic to {@link com.pranavgupta.todoapp.service}.
 * Request-shape validation is expressed via {@code jakarta.validation} annotations
 * on request records.
 */
package com.pranavgupta.todoapp.controller;
