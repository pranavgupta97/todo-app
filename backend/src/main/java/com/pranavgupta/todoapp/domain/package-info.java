/**
 * JPA entities and the value objects / enums they rely on.
 * Entities are mutable (JPA requirement) but should prefer intent-revealing
 * methods over anemic setter chains where practical.
 */
package com.pranavgupta.todoapp.domain;
